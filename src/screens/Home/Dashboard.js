import React from 'react';
import { connect } from 'react-redux';

const ScreensHomeDashboard = props => (
    <div>
        <h1>Home</h1>
    </div>
);

export default connect()(ScreensHomeDashboard);
